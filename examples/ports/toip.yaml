containers:
  sh:
    image: alpine:3
    cmd: sh
    ports:
      # Different port on host and container
      - host: 8080
        container: 80
      # Autogenerated port on host
      - container: 80
      # Same port on host and container
      - host: 3306
      - host: ${CONTAINER_PORT}
        container: 21
